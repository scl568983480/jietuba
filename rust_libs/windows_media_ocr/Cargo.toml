[package]
name = "oneocr-engine"
version = "0.4.0"
edition = "2021"
description = "Windows OneOCR high-accuracy engine bindings for the windows_media_ocr wheel"
license = "MIT"

[lib]
name = "oneocr_engine"
crate-type = ["cdylib", "rlib"]

[dependencies]
pyo3 = { version = "0.22", features = ["extension-module"] }

[workspace]
